# Serveur
PORT=3000
NODE_ENV=

# Database (un seul DB PostgreSQL)
DATABASE_URL=

# Sécurité Auth (JWT pour sessions scalables)
BCRYPT_ROUNDS=
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
ACCESS_TOKEN_TTL_MIN=
REFRESH_TOKEN_TTL_DAYS=

# Email (pour contact et vérif)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_TO=
PUBLIC_BASE_URL=

# CORS
CORS_ORIGIN=
 # Port Vite React

# Rate Limit (scalabilité: 100 req/min/IP pour auth, 500 pour quiz)
RATE_LIMIT_WINDOW=
RATE_LIMIT_AUTH_MAX=
RATE_LIMIT_QUIZ_MAX=

# Admin (pour notes)
ADMIN_CODE=

# Metrics (optionnel)
PROMETHEUS_PORT=
